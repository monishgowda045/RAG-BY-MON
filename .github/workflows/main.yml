name: My First Workflow
on: [push] # Triggers on every push

jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v4
      - name: Run a greeting
        run: echo "Hello, the workflow is running successfully!"
